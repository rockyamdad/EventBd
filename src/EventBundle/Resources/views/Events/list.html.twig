{% extends '::baseBackend.html.twig' %}
{% block body %}
    {% for flashMessage in app.session.flashbag.get('message') %}
        <div class="alert alert-success">
            <button type="button" class="close" data-dismiss="alert" aria-hidden="false"></button>
            <strong> {{ flashMessage }}</strong>
        </div>
    {% endfor %}
    <h3 class="page-title">
        Event Section
    </h3>
    <div class="page-bar">
        <ul class="page-breadcrumb">
            <li>
                <i class="fa fa-home"></i>
                <a href="{{ path('event_dashboard') }}">Home</a>
                <i class="fa fa-angle-right"></i>
            </li>
            <li>
                <a href="{{ path('event_group_list') }}">Event List</a>
            </li>
        </ul>

    </div>
    <div class="portlet box purple">
        <div class="portlet-title">
            <div class="caption">
                <i class="fa fa-cogs"></i>Event List
            </div>
            <div class="actions">

            </div>
        </div>
        <div class="portlet-body">

                 {{ datatable_render_html(datatable) }}

        </div>
    </div>


{% endblock %}
{% block javascriptBlock %}

    {{ parent() }}
    <!-- BEGIN PAGE LEVEL PLUGINS -->
    {% javascripts
    '@EventBundle/Resources/public/js/events.js' %}
    <script type="text/javascript" src="{{ asset_url }}"></script>

    {% endjavascripts %}

    {{ datatable_render_js(datatable) }}
    <!-- END GLOBAL JS -->
{% endblock javascriptBlock %}
